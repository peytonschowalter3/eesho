# Lessons

## 2026-06-29 - Startup Memory Habit

What happened: The user repeated the instruction to maintain `memory/MEMORY.md`, `memory/lessons.md`, and `tasks/todo.md`, and to read all three at the start of every new session.

Why it was wrong: If this instruction only lives in chat, it can be missed after compaction or in a future session. Repetition signals that the startup memory habit is important enough to persist explicitly.

What changed: Added a reinforcement memory file and indexed it in `memory/MEMORY.md`; kept this narrative lesson so repeated reminders become operational practice instead of background noise.

Deeper principle: Persistent workspace rules should be loaded before action, especially when they shape how future corrections, plans, and project context are stored.
